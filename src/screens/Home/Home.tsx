import React, { FC } from 'react';
import { Block, Text } from '~/components';

const Home: FC = () => {
  return (
    <Block center middle>
      <Text>Home Screen</Text>
    </Block>
  );
};

export default Home;
