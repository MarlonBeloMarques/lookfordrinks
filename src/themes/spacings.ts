import { moderateScale } from 'react-native-size-matters';

export default {
  baseSpacing: moderateScale(16),
  topSpacing: moderateScale(10),
  bottomSpacing: moderateScale(10),
  sceneSpacing: moderateScale(24),
  minimumSpacing: moderateScale(4),
  smallSpacing: moderateScale(12),
  mediumSpacing: moderateScale(20),
  largeSpacing: moderateScale(24),
};
