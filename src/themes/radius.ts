import { moderateScale } from 'react-native-size-matters';

export default {
  smallRadius: moderateScale(4),
  mediumRadius: moderateScale(8),
  largeRadius: moderateScale(20),
  buttonRadius: moderateScale(4),
};
